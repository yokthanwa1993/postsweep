{
  "name": "postsweep",
  "version": "1.3.2",
  "private": true,
  "description": "Chrome extension for clearing posts from the current Facebook profile with a calendar, date progress and adjustable speed.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
