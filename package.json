{
  "name": "postsweep",
  "version": "1.2.1",
  "private": true,
  "description": "Chrome extension for clearing Facebook posts with adjustable speed.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
